<div class="col-md-3">
  <aside>
    <section class="user_infor">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div>
    <section class="stats">
      <%= render "shared/stats" %>
    </section>
  </div>
  <%= render "follow_form" if logged_in? %>
</div>
